# StudyPal 设计规范 v5.0

## 产品定位

StudyPal 是一款面向泛学习者的 AI 学习搭子应用，核心特点是有情绪反应的虚拟学习伴侣。产品调性：**温暖、治愈、陪伴感**，让用户感受到"有人在关心我的学习"。

---

## 设计理念

### 场景设定
用户（考研/考证/考公人群）在深夜、在图书馆、在书桌前独自学习时，打开 StudyPal，希望得到：
- 一个温暖的陪伴者
- 一个记录心情的私密空间
- 一点学习的动力和鼓励

### 设计关键词
- **温暖治愈**：柔和的渐变、圆润的卡片、适度的留白
- **卡片式布局**：拒绝传统表单，用卡片承载信息和操作
- **视觉层级**：通过大小、颜色、间距区分信息优先级
- **移动优先**：优先考虑手机端体验，确保单手操作舒适

### 颜色策略
采用 **Restrained + Committed** 混合策略：
- 主色调（薄荷绿 #00D9B1）：代表活力与专注
- 强调色（珊瑚粉 #FF9A9E）：温暖治愈，用于心情、日记等情感功能
- 背景色：根据主题切换，深色优雅、浅色明亮

### 字体
- 标题：`Nunito`（圆润友好）或 `Noto Sans SC`
- 正文：`Noto Sans SC`
- 禁用纯衬线字体，保持现代感

---

## 色彩系统

### 浅色模式
```css
--bg-base: #FAFAFA;           /* 页面背景 */
--bg-surface: #FFFFFF;         /* 卡片背景 */
--bg-elevated: #F5F5F7;       /* 浮动元素 */
--text-primary: #1A1A1A;       /* 主文字 */
--text-secondary: #6B7280;    /* 次要文字 */
--text-muted: #9CA3AF;         /* 辅助文字 */
```

### 深色模式
```css
--bg-base: #0F0F14;            /* 页面背景 */
--bg-surface: #1A1A24;         /* 卡片背景 */
--bg-elevated: #252532;        /* 浮动元素 */
--text-primary: #FAFAFA;       /* 主文字 */
--text-secondary: #A0A0B0;    /* 次要文字 */
--text-muted: #6B6B7B;         /* 辅助文字 */
```

### 语义色
```css
--primary: #00D9B1;            /* 薄荷绿：主要操作、成功 */
--primary-soft: rgba(0, 217, 177, 0.12);
--accent: #FF9A9E;             /* 珊瑚粉：情感、日记、心情 */
--accent-soft: rgba(255, 154, 158, 0.15);
--coral: #FF6B6B;              /* 珊瑚红：警告、错误 */
--sky: #60A5FA;                /* 天蓝：信息、链接 */
--gold: #FBBF24;               /* 金黄：成就、高亮 */
--dusk: #A78BFA;               /* 紫罗兰：特殊状态 */
```

---

## 排版系统

### 字号阶梯
```css
--text-hero: clamp(2rem, 6vw, 3rem);  /* 页面大标题 */
--text-2xl: 1.75rem;                  /* 卡片标题 */
--text-xl: 1.375rem;                  /* 区块标题 */
--text-lg: 1.125rem;                  /* 强调文字 */
--text-base: 1rem;                    /* 正文 */
--text-sm: 0.875rem;                  /* 辅助文字 */
--text-xs: 0.75rem;                   /* 标签、小字 */
```

### 字重
- 400：正文
- 500：辅助信息
- 600：强调、按钮
- 700：标题
- 800：Hero 文字

---

## 圆角系统

```css
--radius-sm: 8px;      /* 小按钮、标签 */
--radius-md: 12px;     /* 输入框、中等卡片 */
--radius-lg: 16px;     /* 大卡片 */
--radius-xl: 24px;     /* 模态框、弹窗 */
--radius-2xl: 32px;    /* 特殊卡片 */
--radius-full: 9999px; /* 胶囊按钮 */
```

---

## 阴影系统

```css
/* 浅色模式 */
--shadow-sm: 0 1px 2px rgba(0, 0, 0, 0.04);
--shadow-md: 0 4px 12px rgba(0, 0, 0, 0.06);
--shadow-lg: 0 8px 24px rgba(0, 0, 0, 0.08);
--shadow-xl: 0 12px 40px rgba(0, 0, 0, 0.1);

/* 深色模式 */
--shadow-sm: 0 1px 2px rgba(0, 0, 0, 0.2);
--shadow-md: 0 4px 12px rgba(0, 0, 0, 0.3);
--shadow-lg: 0 8px 24px rgba(0, 0, 0, 0.4);
--shadow-xl: 0 12px 40px rgba(0, 0, 0, 0.5);

/* 特色阴影 */
--shadow-glow: 0 0 24px rgba(0, 217, 177, 0.15);
--shadow-warm: 0 4px 20px rgba(255, 154, 158, 0.25);
```

---

## 布局系统

### 页面结构
```
┌─────────────────────────────────────┐
│           Top Bar (60px)            │  顶部栏：问候语 + 日期
├─────────────────────────────────────┤
│                                     │
│           Main Content              │  主内容区：自适应
│           (卡片网格)                 │
│                                     │
├─────────────────────────────────────┤
│          Bottom Nav (68px)          │  底部导航
└─────────────────────────────────────┘
```

### 卡片网格（首页）
- 移动端：单列堆叠
- 平板：双列
- 桌面：三列

### 历史日记卡片流
- Masonry 瀑布流布局
- 或单列大卡片（推荐，更温暖）

---

## 组件规范

### 1. 卡片组件
```html
<div class="card">
    <div class="card-header">标题</div>
    <div class="card-body">内容</div>
    <div class="card-footer">操作</div>
</div>
```
- 圆角：radius-lg (16px)
- 背景：bg-surface
- 阴影：shadow-md
- 内边距：16px 或 20px

### 2. 心情选择器
- 5个心情选项横向排列
- 选中状态：放大 + 边框高亮
- 心情 emoji 尺寸：32-36px
- 点击时带轻微弹性动画

### 3. 日记编辑器
- **不要用传统表单**
- 标题：全宽输入，大字号
- 内容：自动增高 textarea
- 标签：胶囊式标签云
- 天气：图标按钮组
- 图片：网格预览 + 添加按钮

### 4. 历史日记卡片
- 左侧心情色条标识
- 右上角心情 emoji
- 日期 + 时间
- 标题（加粗）
- 内容预览（3行截断）
- 标签流
- 底部：图片缩略图 + 更多操作

### 5. 底部导航
- 5个导航项
- 居中图标 + 下方文字
- 选中态：图标变色 + 轻微放大
- 高度：68px（含安全区）

### 6. 按钮
- 主要按钮：渐变背景（primary）
- 次要按钮：描边样式
- 文字按钮：无背景
- 圆角：radius-full 或 radius-md

---

## 动效规范

### 过渡时长
- 快：150ms（hover、微交互）
- 中：250ms（展开、滑入）
- 慢：400ms（页面切换）

### 缓动曲线
```css
--ease-out: cubic-bezier(0.16, 1, 0.3, 1);
--ease-spring: cubic-bezier(0.34, 1.56, 0.64, 1);
```

### 推荐动效
- 卡片 hover：translateY(-2px) + 阴影增强
- 心情选择：scale(1.1) + 弹性回弹
- 页面加载：fadeIn + slideUp 组合
- Toast：slideUp 滑入 + fadeOut 消失

---

## 页面设计

### 日记页面（diary.html）
1. **顶部**：日期 + 连续记录天数徽章
2. **心情选择器**：5个心情卡片，柔和渐变背景
3. **快速记录卡片**：
   - 大标题输入（placeholder："今天想记录什么..."）
   - 内容 textarea（自动增高）
   - 标签选择（热门标签 + 自定义）
   - 天气 + 图片（可选）
4. **保存按钮**：渐变背景，醒目但不刺眼
5. **历史日记**：
   - 搜索栏
   - 筛选标签
   - 卡片流布局（按日期分组）
   - 无限滚动加载

### 首页（home.html）
1. **搭子 Hero 卡片**：大头像 + 名称 + 状态 + 最近消息
2. **今日统计**：3列小卡片（学习时长、番茄数、目标完成）
3. **快捷操作**：2x2 网格卡片（开始学习、和搭子聊聊、写日记、待办事项）
4. **计时器卡片**：环形进度 + 时间显示 + 学科选择
5. **目标卡片**：进度条 + 目标详情
6. **搭子切换入口**：横向头像滑动

### 聊天页面（chat.html）
1. **侧边栏**：Logo + 新建会话 + 历史会话（可选收起）
2. **搭子信息栏**：头像 + 名称 + 状态 + 模型徽章
3. **消息区域**：
   - 用户消息：右对齐，渐变气泡
   - 搭子消息：左对齐，白色气泡 + 头像
4. **输入区**：
   - 自适应高度 textarea
   - 发送按钮
   - 底部提示

---

## 深色模式特殊处理

1. **背景层次**：
   - 深色模式背景更深邃
   - 使用微妙的渐变增加层次感

2. **文字对比度**：
   - 确保 AA 标准（4.5:1）
   - 重要文字用 --text-primary

3. **阴影调整**：
   - 深色模式阴影更深更明显
   - 可适当添加内阴影增加层次

4. **特殊效果**：
   - 心情选择器在深色模式下保持柔和
   - 避免过亮的渐变

---

## 参考设计

1. **Notion**：干净的卡片、柔和的圆角、简洁的层次
2. **Momo 日记**：温暖的配色、情感化的设计
3. **小红书记录页**：卡片流、大图预览、舒适的阅读体验
4. **Apple Design**：精致的光影、流畅的动效
