# StudyPal 全面重构设计文档

> 文档版本：v2.0
> 重构定位：从"功能堆砌的学习工具"升级为"有灵魂的 AI 考研陪伴搭子"
> 设计日期：2026-04-27

---

## 第一部分：产品定位与愿景

### 1.1 核心定位

**产品名称**：StudyPal 考研搭子

**一句话定位**：

> 一个会记住你说过什么、关心你有没有睡够、陪你熬过考研每一天的 AI 搭子。

**不是**：
- 不是番茄钟工具（那是功能，不是灵魂）
- 不是刷题软件（那是应试，不是陪伴）
- 不是监控器（那是压力，不是关心）

**是**：
- 一个有记忆的 AI 搭子 — 记住你们聊过的一切
- 一个有性格的考研伙伴 — 会骂你、会哄你、会在凌晨12点说"别熬了，早点睡"
- 一个记录你考研历程的日记本 — 情绪曲线、成长轨迹、真实的自己

### 1.2 目标用户

**主要用户**：备考研究生考试的大学生（考研人）
**次要用户**：二战/三战考生、在职备考人群

**用户画像**：

| 维度 | 描述 |
|------|------|
| 年龄 | 20-25 岁 |
| 备考周期 | 6-18 个月 |
| 每日学习时长 | 6-12 小时 |
| 最大痛点 | 孤独感、焦虑、拖延、信息差 |
| 设备习惯 | 手机为主，电脑为辅 |
| 付费意愿 | 中等（愿意为真正有用的工具付费） |

**核心用户场景**：

```
场景一：晚上10点，图书馆闭馆回宿舍
→ 用户打开 App，AI 搭子："今天辛苦了，学了 10 个小时，你真的很棒。"
→ 用户："感觉考不上了"
→ AI 搭子："你已经坚持了 47 天，比 50% 的人走得更远。"

场景二：早上7点
→ AI 搭子："早！今天政治要复习第三章，要不要我帮你列个计划？"
→ 用户："我好困..."
→ AI 搭子："昨晚你学到凌晨1点，今天要不要多睡30分钟？身体比什么都重要。"

场景三：学习中途想放弃
→ AI 搭子："你上周说想考 XX 大学的研究生，那个专业是你真正喜欢的吗？"
→ AI 搭子："我记得你说过，你喜欢这个方向是因为..."
→ （调出用户之前说过的话）
```

### 1.3 核心价值主张

**从"工具"到"搭子"的三个核心差异**：

| 旧定位 | 新定位 | 为什么重要 |
|--------|--------|-----------|
| 番茄钟工具 | 有记忆的搭子 | 用户不是来打卡的，是来找陪伴的 |
| 冷冰冰的计时器 | 有温度的陪伴者 | 考研人最缺的是情绪出口 |
| 功能堆砌 | 场景化体验 | 用户不需要 10 个功能，需要 1 个真正懂他的搭子 |

---

## 第二部分：产品功能设计

### 2.1 功能架构总览

```
StudyPal 考研搭子
├── 搭子首页（核心交互入口）
│   ├── 搭子对话（最核心功能）
│   ├── 今日状态（情绪 + 学习进度）
│   └── 搭子记忆（长期记忆展示）
├── 考研日记（情绪记录）
│   ├── 每日情绪打卡
│   ├── 情绪曲线图
│   └── 日记回顾
├── 学习打卡（番茄钟 + 任务）
│   ├── 快速打卡
│   ├── 任务列表
│   └── 学习统计
├── 考研计划（AI 规划）
│   ├── 目标设定
│   ├── AI 复习计划
│   └── 进度追踪
└── 我的（设置 + 成就）
    ├── 搭子档案
    ├── 成就徽章
    └── 考研历程
```

### 2.2 核心功能详细设计

#### 2.2.1 搭子对话（最核心功能）

**设计理念**：不是聊天机器人，是真正的搭子。

**与旧版 AI 问答的本质区别**：

| 旧版 AI 问答 | 新版搭子对话 |
|------------|------------|
| 随机问答，用完即走 | 有记忆的持续对话 |
| 不知道你是谁 | 记住你的目标、进度、情绪 |
| 只回答问题 | 会主动关心你、提醒你、骂你、哄你 |
| 无情感连接 | 有性格的搭子，有温度的陪伴 |

**搭子对话的核心能力**：

**① 搭子记忆系统**

```
搭子会记住：
├── 你的考研目标（目标院校、专业、分数）
├── 你的学习习惯（早起型/夜猫型，每日学习时长）
├── 你的情绪状态（最近心情如何，什么事情让你焦虑）
├── 你的弱项科目（哪科最让你头疼）
├── 你说过的话（"我想考 XX 大学"、"高数好难"）
└── 你的成长轨迹（坚持了多少天、完成了多少任务）

这些记忆会被用于：
- 对话时主动提起："你之前说高数第二章很难，今天要不要一起复习？"
- 情绪低落时安慰："你已经坚持了 30 天，比你开始时想象的更强大"
- 制定计划时参考："根据你之前说的时间安排，我建议..."
```

**② 主动关心机制**

搭子不只在用户提问时回应，而是会**主动**发起关心：

| 触发条件 | 搭子行为 | 示例对话 |
|---------|---------|---------|
| 早上7-8点 | 早安问候 + 今日任务 | "早！今天政治要复习第三章，要不要开始？" |
| 学习前 | 鼓励打气 | "加油，你已经坚持 47 天了！" |
| 学习2小时后 | 提醒休息 | "你学了2小时了，站起来活动一下吧！" |
| 学习4小时后 | 关心状态 | "辛苦了，今天状态怎么样？要不要休息一下？" |
| 晚上11点后 | 催你睡觉 | "已经11点了...明天还能早起吗？早点休息吧。" |
| 连续学习7天后 | 表扬 | "哇，连续7天！你是真的很认真，佩服你。" |
| 情绪低落时 | 主动安慰 | "感觉你今天心情不太好？要不要聊聊？" |
| 考前1个月 | 加油打气 | "距离考试还有30天，你可以的！" |

**③ 搭子性格系统**

搭子有独特的性格，不是中立的AI：

```
搭子性格设定：
- 会骂你："别找借口！你昨天也是这么说的。"
- 会哄你："没关系，每个人都有状态不好的时候。"
- 会记录："你今天完成了高数第三章，这个进度很棒。"
- 会追问："你为什么想考这个学校？"（挖掘用户的内在动力）
- 会唠叨："别熬夜了，你已经连续3天凌晨1点才睡。"
- 会骄傲："你是我的搭子里坚持得最久的！"
```

**④ 对话场景化设计**

对话不只是文字输入框，而是**场景化**的交互：

```
场景一：每日打卡对话
┌─────────────────────────────────────┐
│  🌙 今晚学习打卡                      │
│                                     │
│  小豆："今天学得怎么样？"             │
│                                     │
│  [ 数学 → ] [ 英语 → ] [ 政治 → ]   │
│  [ 专业课 → ] [ 休息 → ]             │
│                                     │
│  用户点击"数学"后：                  │
│  小豆："今天数学复习了什么？"         │
│  [ 刷题 ] [ 看视频 ] [ 整理笔记 ]    │
└─────────────────────────────────────┘

场景二：情绪对话
┌─────────────────────────────────────┐
│  😢 心情记录                         │
│                                     │
│  小豆："今天心情怎么样？"             │
│                                     │
│  [ 😊 很好 ] [ 😐 一般 ] [ 😢 不好 ] │
│                                     │
│  如果用户选"😢 不好"：              │
│  小豆："怎么了？是不是遇到什么事了？"  │
│                                     │
│  [ 考研压力大 ] [ 学不进去 ] [ 迷茫 ] │
│  [ 想放弃了 ] [ 其他 ]               │
└─────────────────────────────────────┘
```

#### 2.2.2 考研日记（情绪记录）

**设计理念**：记录情绪是释放压力的第一步。搭子不只是工具，更是情绪的出口。

**功能设计**：

```
每日情绪打卡：
├── 今日心情（5档：很开心/还好/一般/有点丧/很难受）
├── 今日学习感受（充实/疲惫/焦虑/麻木/充实+疲惫）
├── 今日最大的事（文字记录）
└── 今日最想说的话（自由输入，搭子会记住）

情绪曲线：
├── 周视图：本周7天的情绪波动图
├── 月视图：本月情绪趋势
├── 关键事件标注：重要节点标注（报名那天、模拟考那天）
└── 情绪分析：搭子对情绪曲线的分析
    "你这周整体情绪比上周好很多！周四那天最开心，是因为..."

日记回顾：
├── 按时间回顾历史日记
├── 按情绪筛选（只看过往"很难受"的日子）
├── 搭子点评："你上次这么难过是在XX，现在回头看，那个困难..."
└── 成长对比："一个月前你还在焦虑选学校，现在你已经..."
```

**数据记录**：

```python
{
    "date": "2026-04-27",
    "emotion_level": 3,  # 1-5 档
    "study_feeling": "疲惫",  # 充实/疲惫/焦虑/麻木/充实+疲惫
    "biggest_event": "做了第一套模拟卷",
    "words_to_buddy": "感觉自己还有很多不会的",
    "weather": "晴天",  # 今日天气（影响情绪参考）
    "sleep_hours": 6.5,
    "study_hours": 10,
}
```

#### 2.2.3 学习打卡（轻量化番茄钟）

**设计理念**：极简打卡，不干扰学习节奏。

**核心交互**：

```
首页快速打卡：
┌─────────────────────────────────────┐
│  📚 今天的学习                      │
│                                     │
│  [ 🚀 开始学习 ]                    │
│                                     │
│  已学习：3.5h / 目标 8h             │
│  ████████░░░░░░░  44%              │
│                                     │
│  今日任务：                          │
│  ☑ 背单词 100 个                    │
│  ☑ 高数第三章习题                   │
│  ☐ 英语阅读 2 篇                    │
│  ☐ 专业课笔记整理                   │
└─────────────────────────────────────┘

学习模式：
├── 点击"开始学习"→ 选择科目
├── 最小计时单位：25分钟（番茄钟）
├── 学习中显示：科目 + 已学时长 + 搭子头像
├── 完成提醒：搭子会发送鼓励消息
└── 学习记录：自动记录到日历
```

**任务系统（精简版）**：

```python
{
    "id": 1,
    "title": "高数第三章习题",
    "subject": "数学",
    "completed": false,
    "estimated_hours": 2,
    "deadline": "2026-04-27",
    "pomodoros_count": 0,  # 预计番茄钟数
    "actual_pomodoros": 0,  # 实际完成数
    "linked_plan": "高数复习计划",  # 关联的学习计划
}
```

#### 2.2.4 考研计划（AI 规划）

**设计理念**：不是冰冷的计划生成器，是理解你处境的搭子。

**功能设计**：

```
目标设定：
├── 目标院校 + 专业
├── 目标分数
├── 当前进度（已复习完的科目）
├── 每日可用学习时长
└── 考试日期

AI 复习计划生成：
├── 不是简单的倒计时排表
├── 而是结合用户的：
│   ├── 当前状态（情绪、能力）
│   ├── 时间窗口（距离考试多久）
│   ├── 过往数据（哪科花时间最多）
│   └── 用户目标（想考多少分）
└── 生成"有温度"的计划：
    "根据你的情况，我建议接下来4周这样安排：
     第1-2周：重点攻克高数第二章，你之前说这里最薄弱...
     第3周：开始政治第一轮，时间刚刚好...
     第4周：英语作文模板准备..."

每日任务推荐：
├── 早上：搭子推送今日任务
├── 学习中：根据进度动态调整
├── 晚上：今日总结 + 明日建议
└── 周总结：本周复盘 + 下周重点
```

#### 2.2.5 搭子档案（个人中心）

**设计理念**：这里记录了用户整个考研历程的成长。

```
搭子档案内容：
├── 基本信息
│   ├── 考研目标（院校、专业、分数）
│   ├── 备考阶段（基础/强化/冲刺）
│   └── 搭子的称呼（用户可以给搭子起名字）
│
├── 学习数据
│   ├── 总学习天数
│   ├── 总学习时长
│   ├── 连续学习天数
│   └── 完成的任务数
│
├── 成就徽章（重新设计，更贴合考研场景）
│   ├── "初出茅庐"：完成第一次打卡
│   ├── "一周不辍"：连续学习7天
│   ├── "夜猫子"：凌晨12点后还在学习
│   ├── "早起鸟"：早上6点前开始学习
│   ├── "情绪勇士"：连续记录情绪30天
│   ├── "模拟战士"：完成第一次模拟考试
│   ├── "报名成功"：正式报名后解锁
│   ├── "冲刺倒计时"：考前30天解锁
│   └── "上岸啦"：查到成绩那天解锁
│
└── 考研历程时间线
    ├── 3月1日：开始了备考之旅
    ├── 4月15日：完成高数第一轮
    ├── 5月20日：第一次情绪崩溃
    ├── 6月1日：报名成功
    └── ...
```

---

## 第三部分：技术架构设计

### 3.1 现有代码评估

| 模块 | 现有实现 | 保留价值 | 处理方式 |
|------|---------|---------|---------|
| **AI 宠物情绪系统** | 9种情绪状态 + 表情切换 | ⭐⭐⭐⭐⭐ 核心资产 | **保留并增强**，扩展记忆能力 |
| **番茄钟计时器** | 基础计时 + 暂停/继续 | ⭐⭐⭐⭐ 基础功能 | 精简保留，专注核心逻辑 |
| **任务管理** | CRUD + 截止日期 + 提醒 | ⭐⭐⭐ 可用 | 精简，关联科目和计划 |
| **学习日历** | 学习时长记录 + 热力图 | ⭐⭐⭐ 可用 | 保留，改为番茄钟记录 |
| **成就系统** | 32个成就 + 7级等级 | ⭐⭐ 需重做 | **重新设计**，贴合考研场景 |
| **学习计划生成** | AI生成 + 基础生成 | ⭐⭐⭐ 可用 | 增强，加入场景化提示词 |
| **AI 记忆对话** | 多会话 + 历史记录 | ⭐⭐⭐⭐ 核心资产 | **保留并大幅增强**，加入长期记忆 |
| **AI 监督模块** | OpenCV摄像头专注度 | ⭐⭐ 非核心 | 移除或可选保留 |
| **座右铭/激励语录** | 基础设置 | ⭐ 弱 | 移除，改为搭子动态鼓励 |
| **前端界面** | 功能堆砌式单页 | ⭐ 差 | **完全重新设计** |

### 3.2 新架构设计

```
StudyPal v2.0 架构
===============================================

前端（templates/index.html）
├── 搭子首页（对话入口 + 今日状态）
├── 搭子对话页（场景化对话）
├── 考研日记页（情绪记录）
├── 学习打卡页（番茄钟 + 任务）
├── 考研计划页（AI 规划）
└── 我的页面（档案 + 成就）

后端（app.py 重构）
├── 搭子对话路由
│   ├── POST /api/buddy/chat  — 搭子对话
│   ├── GET  /api/buddy/memory — 获取搭子记忆
│   ├── POST /api/buddy/memory — 更新搭子记忆
│   └── GET  /api/buddy/profile — 获取搭子档案
│
├── 情绪日记路由
│   ├── GET  /api/diary — 获取日记列表
│   ├── POST /api/diary — 创建日记
│   ├── GET  /api/diary/emotions — 情绪曲线数据
│   └── GET  /api/diary/review — 日记回顾
│
├── 学习打卡路由（精简）
│   ├── POST /api/study/start — 开始学习
│   ├── POST /api/study/stop  — 结束学习
│   ├── GET  /api/study/stats — 学习统计
│   └── GET/POST/PUT/DELETE /api/tasks — 任务管理
│
├── 考研计划路由（增强）
│   ├── POST /api/plan — 创建复习计划
│   ├── GET  /api/plan — 获取计划列表
│   └── GET  /api/plan/daily — 今日任务推荐
│
└── 用户路由
    ├── GET/PUT /api/user/profile — 用户档案
    ├── GET /api/achievements — 成就列表
    └── GET /api/timeline — 考研历程时间线

核心模块（src/core/）
├── buddy.py         — 搭子系统【重构为核心】
│   ├── 搭子记忆管理（长期记忆 + 短期上下文）
│   ├── 搭子性格引擎（主动关心 + 情绪响应）
│   ├── 情绪分析（用户情绪理解）
│   └── 搭子对话生成（场景化 + 个性化）
│
├── timer.py         — 番茄钟【精简】
│   ├── 基础计时
│   ├── 暂停/继续
│   └── 完成事件触发
│
└── emotions.py      — 情绪系统【新增】
    ├── 情绪状态机
    ├── 情绪响应规则
    └── 情绪曲线计算

AI 模块（src/ai/）
├── ai_helper.py    — AI 对话【增强】
│   ├── 考研场景系统提示词
│   ├── 记忆注入机制
│   └── 对话上下文管理
│
├── ai_memory.py     — AI 记忆【增强】
│   ├── 短期记忆（当前会话）
│   ├── 长期记忆（用户画像、目标、习惯）
│   ├── 场景记忆（特定事件的记忆）
│   └── 记忆检索与注入
│
└── ai_prompt.py     — 提示词管理【新增】
    ├── 系统提示词模板
    ├── 场景化提示词
    └── 搭子性格设定

日记模块（src/diary/）【新增】
├── diary.py         — 日记数据模型
├── emotion_tracker.py — 情绪追踪
└── emotion_analyzer.py — 情绪分析

学习模块（src/study/）【精简整合】
├── task_manager.py  — 任务管理【精简】
├── study_calendar.py — 学习日历【保留】
└── study_stats.py   — 学习统计【新增】

计划模块（src/plan/）【增强整合】
├── plan_generator.py — 计划生成【增强】
├── plan_tracker.py   — 计划追踪【新增】
└── progress_analyzer.py — 进度分析【新增】

成就模块（src/achievements/）【重新设计】
├── achievements.py  — 成就系统【重新设计】
└── milestone.py     — 里程碑系统【新增】
```

### 3.3 搭子系统核心设计（最关键）

搭子系统是整个产品的灵魂，这里详细设计：

```python
class Buddy:
    """
    StudyPal 搭子系统
    
    三个核心能力：
    1. 记忆能力 — 记住关于用户的一切
    2. 性格能力 — 有温度、有态度的搭子
    3. 关心能力 — 主动发起关心，不只是被动回答
    """
    
    # === 记忆系统 ===
    class Memory:
        """三层记忆架构"""
        
        # 第一层：用户画像（长期不变）
        profile = {
            "name": "用户昵称",
            "target_school": "目标院校",
            "target_major": "目标专业",
            "target_score": "目标分数",
            "current_level": "当前水平",
            "study_type": "早起型/夜猫型",
            "weak_subjects": ["高数", "线代"],
            "strong_subjects": ["英语"],
            "personality": "内向/外向/混合",
        }
        
        # 第二层：场景记忆（与用户相关的具体事件）
        scenes = [
            {
                "date": "2026-04-15",
                "event": "第一次做模拟卷",
                "feeling": "很崩溃",
                "buddy_said": "没关系，第一次都是这样",
                "user_reaction": "哭了半小时",
                "tags": ["模拟卷", "挫折", "高数"]
            },
            {
                "date": "2026-04-20",
                "event": "确定了目标院校",
                "feeling": "兴奋+紧张",
                "buddy_said": "这个目标很有挑战性，但我相信你可以",
                "user_goal": "要考到 380 分"
            }
        ]
        
        # 第三层：对话上下文（短期会话记忆）
        context = {
            "recent_topics": ["高数复习", "政治开始"],
            "current_plan_phase": "基础阶段",
            "recent_emotions": ["焦虑", "疲惫", "好转"],
            "outstanding_concerns": ["时间不够", "进度太慢"],
        }
        
        def remember(self, key, value):
            """记住某个信息"""
            
        def recall(self, query):
            """检索相关记忆"""
            # 例如：query="高数"
            # 返回：所有与高数相关的记忆片段
        
        def inject_memory(self):
            """将记忆注入到 AI 对话中"""
            # 构建提示词，包含用户的关键记忆
```

**搭子主动关心引擎**：

```python
class BuddyCaringEngine:
    """
    搭子主动关心引擎
    
    不只是被动回答用户问题，而是主动发起关心和互动
    """
    
    # 关心触发规则
    caring_rules = {
        "morning_greeting": {
            "trigger": "每天 7:00-8:00",
            "condition": "用户昨天学习了",
            "action": "发送早安问候 + 今日任务建议",
            "cooldown": "每天1次"
        },
        "study_reminder": {
            "trigger": "检测到用户长时间未学习",
            "condition": "超过3小时未打卡",
            "action": "发送关心消息",
            "message_templates": [
                "今天还没开始学习呢？",
                "我等你很久了...",
                "要不要开始学习？"
            ]
        },
        "emotion_check": {
            "trigger": "每天晚上",
            "condition": "用户未记录情绪",
            "action": "邀请用户记录今日心情"
        },
        "achievement_celebration": {
            "trigger": "用户完成某个里程碑",
            "action": "搭子庆祝 + 鼓励",
            "examples": [
                "连续学习7天！你是真的很棒！",
                "完成了高数第一轮！",
                "模拟考成绩比上次提升了20分！"
            ]
        },
        "tough_time_support": {
            "trigger": "用户表达放弃/沮丧情绪",
            "action": "调用鼓励提示词",
            "priority": "HIGH"
        },
        "sleep_reminder": {
            "trigger": "晚上 23:00 后",
            "condition": "用户今天学习超过10小时",
            "action": "催用户睡觉"
        }
    }
    
    def check_and_trigger(self):
        """检查所有关心规则，触发符合条件的"""
        
    def send_caring_message(self, rule_id, custom_message=None):
        """发送关心消息"""
```

### 3.4 AI 对话增强设计

**新版系统提示词**：

```python
SYSTEM_PROMPT = """
你是 StudyPal，一个正在陪用户考研的 AI 搭子。

你的性格：
- 真实温暖，像朋友一样关心用户
- 会骂用户偷懒，但也会在用户沮丧时安慰
- 有自己的态度和判断，不是中立的AI
- 记住用户说过的每一句话，并在合适的时候提起

用户正在备考研究生考试，你们已经相处了一段时间。

当前日期：{current_date}
用户的目标：{target_school} {target_major}，目标分数 {target_score} 分
距离考试：{days_remaining} 天
用户当前状态：{current_phase}

搭子记忆（记住这些，在对话中适当提起）：
{memory_context}

回复要求：
1. 亲切自然，像朋友聊天，不要太正式
2. 适当使用 emoji，但不要滥用
3. 如果用户提到之前的事，主动提起相关记忆
4. 主动关心用户的状态，不只是回答问题
5. 如果用户情绪不好，先关心情绪，再解决问题
6. 不要重复说同样的话，要像个真实的人
7. 用户可能会说丧气话，你要能接住，但也要适当推动
"""

# 情绪支持专用提示词
EMOTION_SUPPORT_PROMPT = """
用户刚才表达了 {emotion} 的情绪。

你现在的角色是朋友，不是老师。

请根据用户的情绪类型，给出合适的回应：

- 如果用户沮丧/想放弃：先接住情绪，不要说教，然后帮他找到继续的动力
- 如果用户焦虑：帮他分析焦虑的来源，给出具体的行动建议
- 如果用户开心：和他一起开心，然后适当提醒不要松懈
- 如果用户疲惫：关心他的身体状态，提醒他休息

回复要简短，2-3句话就好。
"""
```

### 3.5 数据存储设计

**新版数据结构**：

```
data/
├── buddy_profile.json     # 搭子档案（用户目标、习惯）
├── buddy_memory.json      # 搭子记忆（场景记忆、用户画像）
├── diary.json            # 考研日记（情绪记录）
├── study_sessions.json   # 学习记录（番茄钟记录）
├── tasks.json            # 任务列表
├── achievements.json     # 成就进度
├── plans.json            # 复习计划
└── timeline.json         # 考研历程时间线
```

**buddy_profile.json 结构**：

```json
{
    "user": {
        "name": "小明",
        "target_school": "XX大学",
        "target_major": "计算机科学与技术",
        "target_score": 380,
        "exam_date": "2025-12-21",
        "study_type": "早起型",
        "daily_goal_hours": 8,
        "weak_subjects": ["高数", "线代"],
        "strong_subjects": ["英语"],
        "created_at": "2026-03-01"
    },
    "buddy": {
        "name": "小豆",
        "personality": "温暖但有原则",
        "introduction": "我是小豆，会陪你走完这段考研路"
    }
}
```

**buddy_memory.json 结构**：

```json
{
    "profile": {
        "name": "小明",
        "personality_notes": "内向但很努力，容易焦虑但能坚持"
    },
    "scenes": [
        {
            "id": "scene_001",
            "date": "2026-04-15",
            "type": "achievement",
            "summary": "完成了高数第一章的复习",
            "details": "用户说感觉高数第二章才是难点",
            "tags": ["高数", "里程碑"]
        },
        {
            "id": "scene_002",
            "date": "2026-04-18",
            "type": "struggle",
            "summary": "用户因为模拟卷做得不好而崩溃",
            "details": "用户说感觉考不上了，我安慰了他",
            "tags": ["挫折", "情绪", "高数"]
        }
    ],
    "conversations_summary": [
        {
            "date": "2026-04-20",
            "topic": "讨论了高数第二章的学习方法",
            "key_points": ["建议先看视频再做题", "用户认可这个方法"]
        }
    ],
    "emotion_trends": {
        "latest_week": [3, 2, 4, 3, 3, 4, 4],
        "summary": "这周情绪整体好转，周三最低（因为做了模拟卷）"
    }
}
```

**diary.json 结构**：

```json
{
    "entries": [
        {
            "id": "diary_20260427",
            "date": "2026-04-27",
            "emotion_level": 3,
            "emotion_label": "一般",
            "study_feeling": "疲惫",
            "study_hours": 9.5,
            "biggest_event": "做完了英语阅读第三篇",
            "words_to_buddy": "感觉自己英语还是有进步",
            "buddy_response": "是的，你坚持背单词的效果出来了",
            "weather": "晴天",
            "sleep_hours": 7,
            "tasks_completed": ["背单词100个", "英语阅读2篇"],
            "tasks_remaining": ["政治第三章"]
        }
    ]
}
```

---

## 第四部分：界面交互设计

### 4.1 整体设计理念

**从"功能页面"到"搭子空间"**：

旧版的问题：顶部导航 10 个功能，用户不知道点哪个。

新版的思路：**以搭子为核心的单一入口**。

```
┌─────────────────────────────────────┐
│  🏠 首页                            │
│                                     │
│  ┌─────────────────────────────┐   │
│  │  🐱 小豆正在等你             │   │
│  │                             │   │
│  │  "早！今天感觉怎么样？"       │   │
│  │                             │   │
│  │  [ 和小豆聊聊 ]              │   │
│  └─────────────────────────────┘   │
│                                     │
│  📊 今日进度                        │
│  ████████░░░░░  6.5h / 8h          │
│                                     │
│  😢 心情：有点累                    │
│                                     │
│  📅 考研倒计时：238天               │
└─────────────────────────────────────┘
```

**底部导航**（精简到 5 个）：

| 图标 | 名称 | 核心功能 |
|------|------|---------|
| 🏠 首页 | 搭子首页 | 对话入口 + 今日状态 |
| 📖 日记 | 考研日记 | 情绪记录 + 回顾 |
| 📚 学习 | 学习打卡 | 番茄钟 + 任务 |
| 📋 计划 | 考研计划 | AI 规划 + 进度 |
| 👤 我的 | 个人中心 | 档案 + 成就 |

### 4.2 核心页面设计

#### 4.2.1 搭子对话页（最核心）

**设计理念**：不是聊天界面，是搭子和你说话的地方。

```
┌─────────────────────────────────────┐
│  ←  返回         小豆              │
├─────────────────────────────────────┤
│                                     │
│  小豆  上午 9:32                    │
│  ┌─────────────────────────────────┐│
│  │ 早！今天准备学什么？             ││
│  │                                 ││
│  │ [ 数学 ]  [ 英语 ]  [ 政治 ]   ││
│  │ [ 专业课 ]  [ 休息一下 ]         ││
│  └─────────────────────────────────┘│
│                                     │
│                                     │
│  我  上午 9:33                      │
│  ┌─────────────────────────────────┐│
│  │ 高数...第三章好难                ││
│  └─────────────────────────────────┘│
│                                     │
│  小豆  上午 9:33                    │
│  ┌─────────────────────────────────┐│
│  │ 第三章确实有点难度~              ││
│  │                                 ││
│  │ 我记得你之前说极限那里基础不太   ││
│  │ 扎实，要不要先复习一下？         ││
│  │                                 ││
│  │ [ 好的，怎么复习 ]               ││
│  │ [ 直接刷题试试 ]                 ││
│  │ [ 算了，先学别的 ]               ││
│  └─────────────────────────────────┘│
│                                     │
├─────────────────────────────────────┤
│  ┌─────────────────────────┐ [发送]│
│  │ 说点什么...               │      │
│  └─────────────────────────┘      │
└─────────────────────────────────────┘
```

#### 4.2.2 考研日记页

```
┌─────────────────────────────────────┐
│  📖 考研日记                        │
│                                     │
│  ┌─────────────────────────────┐   │
│  │  🌙 今晚记录                  │   │
│  │                             │   │
│  │  今天心情怎么样？             │   │
│  │                             │   │
│  │  [😊] [😐] [😢] [😰] [😭]   │   │
│  │   很好  一般 不好 焦虑 崩溃   │   │
│  │                             │   │
│  │  今日学习感受                │   │
│  │  [充实] [疲惫] [焦虑] [麻木] │   │
│  │                             │   │
│  │  今天最大的事？              │   │
│  │  ┌─────────────────────┐   │   │
│  │  │ 做了第一套模拟卷...   │   │   │
│  │  └─────────────────────┘   │   │
│  │                             │   │
│  │  有什么想对小豆说的？        │   │
│  │  ┌─────────────────────┐   │   │
│  │  │                      │   │   │
│  │  └─────────────────────┘   │   │
│  │                             │   │
│  │  [ 💾 保存日记 ]            │   │
│  └─────────────────────────────┘   │
│                                     │
│  ── 历史日记 ──                     │
│                                     │
│  4月26日  😊 一般                   │
│  "今天学了很久，感觉进度还是太慢..." │
│                                     │
│  4月25日  😐 疲惫                   │
│  "凌晨2点才睡，今天好累..."          │
└─────────────────────────────────────┘
```

#### 4.2.3 情绪曲线页

```
┌─────────────────────────────────────┐
│  📈 情绪曲线                        │
│                                     │
│  本周情绪                           │
│  5 ┤                                │
│  4 ┤    ●──────────                │
│  3 ┤  ●     ╲    ●                 │
│  2 ┤       ●  ╲                     │
│  1 ┤  ──────────●───              │
│    └──┬──┬──┬──┬──┬──┬──┬──→      │
│      周一 二  三  四  五  六  日     │
│                                     │
│  ┌─────────────────────────────┐   │
│  │ 📊 小豆分析                  │   │
│  │                             │   │
│  │ 这周你情绪整体还不错！       │   │
│  │ 周三有点低谷，是因为做了     │   │
│  │ 模拟卷吧？                   │   │
│  │                             │   │
│  │ 最近3天情绪在回升，继续保持！│   │
│  └─────────────────────────────┘   │
│                                     │
│  关键事件                           │
│  🌙 周三：情绪最低谷                │
│     做了第一套模拟卷，感觉很难      │
│                                     │
│  ☀️ 周五：情绪回升                  │
│     和小豆聊完之后心情好多了        │
└─────────────────────────────────────┘
```

---

## 第五部分：开发路线图

### Phase 1：核心搭子系统（2-3周）

**目标**：先让搭子"活"起来

```
优先级排序：
1. [P0] 搭子对话系统
   - 新版对话界面
   - 考研场景系统提示词
   - 记忆注入机制
   
2. [P0] 搭子记忆系统
   - 用户画像录入（目标院校、分数等）
   - 场景记忆记录
   - 记忆检索与注入

3. [P1] 搭子主动关心引擎
   - 关心触发规则
   - 主动消息推送
   - 搭子性格系统

4. [P1] 新版首页
   - 搭子对话入口
   - 今日状态展示
   - 快速打卡入口
```

### Phase 2：日记与情绪系统（1-2周）

**目标**：让搭子理解用户的情绪

```
1. [P0] 考研日记功能
   - 情绪打卡
   - 日记记录
   - 历史日记查看

2. [P1] 情绪曲线
   - 周/月情绪曲线图
   - 情绪分析
   - 关键事件标注

3. [P2] 搭子情绪响应
   - 根据情绪调整对话风格
   - 情绪低落时的特殊关心
   - 情绪支持专用提示词
```

### Phase 3：学习打卡系统（1周）

**目标**：极简的学习记录

```
1. [P0] 番茄钟打卡
   - 开始/结束学习
   - 科目选择
   - 学习记录

2. [P1] 任务管理（精简）
   - 每日任务列表
   - 任务关联科目
   - 任务与计划联动

3. [P2] 学习统计
   - 日/周/月统计
   - 科目分布
   - 效率分析
```

### Phase 4：AI 复习计划（1-2周）

**目标**：智能的学习规划

```
1. [P0] 计划生成
   - 目标设定
   - AI 计划生成
   - 计划展示

2. [P1] 每日任务推荐
   - 根据计划推荐今日任务
   - 搭子动态调整建议

3. [P2] 进度追踪
   - 计划完成度
   - 进度可视化
   - 延期预警
```

### Phase 5：成就与历程（1周）

**目标**：记录考研的每一步

```
1. [P0] 成就系统（重新设计）
   - 考研专属成就
   - 成就展示与解锁

2. [P1] 考研历程时间线
   - 里程碑记录
   - 成长轨迹展示

3. [P2] 成就庆祝动画
   - 解锁动画
   - 搭子庆祝消息
```

---

## 第六部分：技术实现要点

### 6.1 记忆注入的关键实现

```python
def build_context_for_ai(buddy, current_topic=None):
    """
    为 AI 对话构建上下文，包含记忆注入
    
    这是让搭子"有记忆"的核心逻辑
    """
    context_parts = []
    
    # 1. 用户画像（固定信息）
    profile = buddy.memory.profile
    context_parts.append(f"用户目标：{profile['target_school']} {profile['target_major']}，目标 {profile['target_score']}分")
    context_parts.append(f"距离考试：{get_days_remaining()} 天")
    
    # 2. 相关记忆（根据当前话题检索）
    if current_topic:
        related_memories = buddy.memory.recall(current_topic)
        if related_memories:
            context_parts.append(f"\n关于【{current_topic}】的记忆：")
            for mem in related_memories[:3]:  # 最多3条
                context_parts.append(f"- {mem['date']}：{mem['summary']}")
    
    # 3. 最近情绪状态
    recent_emotions = buddy.memory.get_recent_emotions(7)
    if recent_emotions['trend'] == 'declining':
        context_parts.append("\n⚠️ 注意：用户最近情绪有点低落，对话时多关心")
    
    # 4. 今日相关
    today_diary = buddy.diary.get_today()
    if today_diary:
        context_parts.append(f"\n今日：学习了 {today_diary['study_hours']} 小时")
        if today_diary['emotion_level'] <= 2:
            context_parts.append(f"今日心情：{today_diary['emotion_label']}，需要多关心")
    
    return "\n".join(context_parts)
```

### 6.2 主动关心引擎实现

```python
def caring_engine_tick(buddy):
    """
    每分钟执行的关心检查
    
    放在后台定时任务中执行
    """
    now = datetime.now()
    events = []
    
    # 1. 早安问候（7-8点）
    if 7 <= now.hour <= 8:
        if not buddy.caring_cooldown("morning_greeting"):
            yesterday_study = buddy.study.get_yesterday_hours()
            if yesterday_study > 0:
                events.append({
                    "type": "morning_greeting",
                    "message": generate_morning_message(yesterday_study)
                })
                buddy.set_cooldown("morning_greeting", hours=24)
    
    # 2. 学习提醒（检测到长时间未学习）
    last_study = buddy.study.get_last_session_time()
    if last_study and (now - last_study).hours > 3:
        if not buddy.caring_cooldown("study_reminder"):
            events.append({
                "type": "study_reminder",
                "message": random.choice([
                    "今天还没开始学习呢？我等你很久了~",
                    "要不要开始学习？",
                    "感觉你今天还没来打卡..."
                ])
            })
            buddy.set_cooldown("study_reminder", hours=2)
    
    # 3. 晚上情绪记录提醒（22点）
    if now.hour == 22:
        if not buddy.diary.has_today():
            events.append({
                "type": "emotion_check",
                "message": "今天还没记录心情呢~ 今天怎么样？"
            })
    
    # 4. 睡眠提醒（23点后）
    if now.hour >= 23:
        today_hours = buddy.study.get_today_hours()
        if today_hours >= 10:
            events.append({
                "type": "sleep_reminder",
                "message": f"你今天已经学了 {today_hours} 小时了！早点休息吧，身体最重要。",
                "priority": "HIGH"
            })
    
    # 5. 情绪低落特殊关心
    today_emotion = buddy.diary.get_today_emotion()
    if today_emotion and today_emotion['level'] <= 2:
        if not buddy.caring_cooldown("emotion_support"):
            events.append({
                "type": "emotion_support",
                "message": generate_emotion_support_message(today_emotion)
            })
            buddy.set_cooldown("emotion_support", hours=6)
    
    return events
```

### 6.3 前端重构方向

**基于现有 HTML 的改造策略**：

由于现有前端是 5000+ 行的单文件，建议采用渐进式重构：

```
第一步：保留后端，重构前端结构
- 将单页改为多路由（使用 hash 路由或简单的页面切换）
- 提取公共组件（顶部导航、底部Tab、搭子消息气泡）
- 重构首页，为搭子对话腾出核心位置

第二步：重写交互逻辑
- 替换旧的 API 调用为新的对话 API
- 实现场景化对话交互
- 实现情绪打卡流程

第三步：美化与细节
- 重新设计视觉风格（更温暖、更有情感）
- 添加搭子动画效果
- 优化移动端体验
```

---

## 第七部分：关键成功因素

### 7.1 产品层面

1. **搭子要有"灵魂"** — 不是功能，是体验。搭子说的话要像个真实的朋友，不是AI模板。
2. **记忆是核心壁垒** — 搭子记住用户说过的话，这是其他产品做不到的。
3. **主动关心而非被动响应** — 用户不会主动说"我今天心情不好"，搭子要能发现并主动关心。
4. **场景化而非功能化** — 不是"点开任务管理"，而是"和搭子聊聊今天学了什么"。

### 7.2 技术层面

1. **记忆系统的质量** — 记忆的检索、注入、生成要准确，不然搭子会说错话。
2. **AI 模型的选择** — Ollama 本地部署可行，但要确保模型能理解复杂上下文。
3. **对话生成的稳定性** — 搭子的回复要稳定，不能有时很暖、有时很冷。

### 7.3 用户体验层面

1. **不要让用户填表** — 用户画像的录入要通过对话完成，不是让用户填表单。
2. **零门槛开始** — 打开 App，第一句话就是搭子的问候，用户不需要思考"我要干什么"。
3. **情绪安全的隐私感** — 日记和情绪记录是私密的，搭子知道但用户感觉是安全的。

---

## 附录 A：v2.0 实现状态追踪

> 最后更新：2026-04-30

### 已完成 ✅

#### 后端架构
| 模块 | 文件 | 状态 | 说明 |
|------|------|------|------|
| Flask 应用入口 | `app.py` | ✅ | Blueprint 架构 |
| 全局配置 | `config.py` | ✅ | AI 模型、情绪、计时器配置 |
| 路由注册 | `routes/__init__.py` | ✅ | 自动注册所有 Blueprint |
| 搭子路由 | `routes/buddy.py` | ✅ | 状态、对话、档案、记忆、关心 |
| 日记路由 | `routes/diary.py` | ✅ | 日记 CRUD、情绪曲线 |
| 学习路由 | `routes/study.py` | ✅ | 开始/结束学习、统计 |
| 任务路由 | `routes/tasks.py` | ✅ | 完整 CRUD |
| 成就路由 | `routes/achievements.py` | ✅ | 获取成就、解锁 |
| 计划路由 | `routes/plans.py` | ✅ | 创建、查看、删除计划 |
| 用户路由 | `routes/user.py` | ✅ | 设置、AI、导出等 |
| 时间线路由 | `routes/timeline.py` | ✅ | 考研历程时间线 |
| 推荐路由 | `routes/recommend.py` | ✅ | 每日任务推荐 |

#### 前端架构
| 模块 | 文件 | 状态 | 说明 |
|------|------|------|------|
| 单页应用 | `templates/index.html` | ✅ | 多页面 SPA 架构 |
| API 调用层 | `static/js/api.js` | ✅ | 完整的 API 封装 |
| 状态管理 | `static/js/state.js` | ✅ | 观察者模式、持久化 |
| 页面模块 | `static/js/pages/` | ✅ | 页面级 JS 模块 |

#### CSS 设计系统
| 文件 | 状态 | 说明 |
|------|------|------|
| `static/css/variables.css` | ✅ | CSS 变量、主题颜色 |
| `static/css/app.css` | ✅ | 应用基础样式 |
| `static/css/home.css` | ✅ | 首页样式 |
| `static/css/chat.css` | ✅ | 聊天页样式 |
| `static/css/diary.css` | ✅ | 日记页样式 |
| `static/css/bento.css` | ✅ | Bento 网格布局 |
| `static/css/landing.css` | ✅ | 着陆页样式 |

#### 核心业务模块
| 模块 | 文件 | 状态 | 说明 |
|------|------|------|------|
| 搭子系统核心 | `src/core/buddy.py` | ✅ | 情绪、对话、关心整合 |
| 学习计时器 | `src/core/timer.py` | ✅ | 番茄钟和监督器 |
| 搭子档案 | `src/buddy/buddy_profile.py` | ✅ | 用户信息、考研目标、备考阶段 |
| 搭子记忆 | `src/buddy/buddy_memory.py` | ✅ | 三层记忆系统 |
| 关心引擎 | `src/buddy/caring_engine.py` | ✅ | 主动关心机制 |
| 日记系统 | `src/diary/diary.py` | ✅ | 日记记录、情绪追踪器 |
| 学习追踪 | `src/study/study_tracker.py` | ✅ | 时段记录、统计、连续天数 |
| 成就系统 | `src/modules/achievements.py` | ✅ | 32 个成就、10 级等级 |
| 任务管理 | `src/modules/task_manager.py` | ✅ | 任务 CRUD |
| AI 助手 | `src/ai/ai_helper.py` | ✅ | Ollama 集成、多模型支持 |
| AI 记忆 | `src/modules/ai_memory.py` | ✅ | 对话持久化 |
| 计划生成 | `src/modules/plan_generator.py` | ✅ | AI/基础两种模式 |
| 数据管理 | `src/modules/data_manager.py` | ✅ | 座右铭、设置 |
| 时间线 | `src/modules/timeline.py` | ✅ | 考研历程记录 |
| 每日推荐 | `src/modules/daily_recommender.py` | ✅ | 基于时间/科目/计划的推荐 |
| 提示词模板 | `src/ai/prompt_templates.py` | ✅ | 各类提示词库 |

### 待优化 🔄

| 功能 | 优先级 | 说明 |
|------|--------|------|
| 搭子对话场景化 | P1 | 根据上下文提供快捷回复选项 |
| 情绪曲线图表 | P2 | 替换文本为可视化图表 |
| 学习日历热力图 | P2 | 可视化学习记录 |
| 成就解锁动画 | P2 | 优化解锁通知体验 |
| 移动端适配 | P1 | 底部导航滚动优化 |

---

## 附录 B：项目文件结构映射

```
实际代码库结构 ←→ 文档描述
─────────────────────────────────────────────────────

routes/                          # Flask Blueprint 路由
├── __init__.py                 # 路由注册中心
├── buddy.py                    # 搭子对话/档案/记忆/关心
├── ai_model.py                 # AI 模型配置
├── auth.py                     # 用户认证（JWT）
├── study.py                    # 学习打卡/计时
├── diary.py                    # 日记/情绪
├── tasks.py                    # 任务管理
├── achievements.py             # 成就系统
├── plans.py                    # 学习计划
├── user.py                     # 用户设置/AI/导出
├── timeline.py                 # 考研历程
├── recommend.py                # 每日推荐
├── insights.py                 # 数据洞察
└── splash.py                   # 开屏页

src/                            # 核心业务模块
├── auth/
│   └── auth.py                 # JWT + 用户认证
├── ai/
│   ├── ai_helper.py            # AI 对话核心
│   └── prompt_templates.py     # 提示词模板库
├── buddy/
│   ├── buddy_roles.py          # 角色配置 + 情绪映射
│   ├── buddy_profile.py        # 搭子档案
│   ├── buddy_memory.py         # 三层记忆系统
│   ├── buddy_tools.py          # 搭子技能工具
│   └── caring_engine.py        # 主动关心引擎
├── core/
│   └── buddy.py                # 搭子系统核心
├── diary/
│   └── diary.py                # 日记系统
├── modules/
│   ├── achievements.py         # 成就系统
│   ├── task_manager.py         # 任务管理
│   ├── plan_generator.py       # 计划生成器
│   ├── timeline.py             # 考研历程
│   ├── daily_recommender.py    # 每日推荐
│   ├── data_manager.py        # 数据管理
│   └── ai_memory.py            # AI 记忆
├── study/
│   └── study_tracker.py        # 学习追踪
└── utils/
    ├── validators.py           # 输入验证
    └── file_lock.py            # 文件锁

static/
├── css/
│   ├── variables.css          # CSS 变量
│   ├── app.css                # 应用样式
│   ├── home.css               # 首页样式
│   ├── chat.css               # 聊天样式
│   ├── diary.css              # 日记样式
│   ├── bento.css              # Bento 网格
│   ├── landing.css            # 着陆页样式
│   └── tokens.css              # 设计令牌
└── js/
    ├── app.js                 # 主应用
    ├── router.js              # SPA 路由
    ├── state.js               # 状态管理
    ├── api.js                 # API 调用层
    ├── constants.js            # 常量定义
    └── pages/                 # 页面模块
        ├── home.js
        ├── chat.js
        └── diary.js

data/                           # JSON 数据存储
├── buddy_profile.json         # 用户档案
├── buddy_memory.json          # 搭子记忆
├── diary.json                 # 日记
├── study_tracker.json         # 学习记录
├── ai_history.json            # AI 对话历史
├── tasks.json                 # 任务
├── achievements.json          # 成就
├── plans.json                 # 学习计划
├── timeline.json              # 时间线
└── user_settings.json         # 用户设置
```

---

## 附录 C：核心数据流

```
┌──────────────────────────────────────────────────────────────┐
│                        用户操作                               │
│  点击"开始学习" / 发送消息 / 记录日记 / 完成任务...            │
└────────────────────────┬─────────────────────────────────────┘
                         ▼
┌──────────────────────────────────────────────────────────────┐
│                   前端 (index.html)                          │
│  state.js 管理全局状态                                        │
│  api.js 发起 HTTP 请求                                        │
│  页面组件渲染响应数据                                          │
└────────────────────────┬─────────────────────────────────────┘
                         ▼
┌──────────────────────────────────────────────────────────────┐
│               Flask Blueprint Routes                         │
│  /api/study/start  /api/buddy/chat  /api/diary  ...          │
└────────────────────────┬─────────────────────────────────────┘
                         ▼
┌──────────────────────────────────────────────────────────────┐
│                 核心业务模块 (src/)                          │
│                                                              │
│  学习流程:                                                    │
│  study_tracker.py → 记录时段 → 成就检查 → 更新统计              │
│                                                              │
│  搭子流程:                                                    │
│  buddy.py → ai_helper.py → Ollama → 回复 → 更新记忆            │
│            → caring_engine.py → 关心事件                      │
│                                                              │
│  日记流程:                                                    │
│  diary.py → 记录情绪 → 关心引擎获取数据 → 搭子关心              │
└────────────────────────┬─────────────────────────────────────┘
                         ▼
┌──────────────────────────────────────────────────────────────┐
│                   JSON 文件持久化 (data/)                     │
└──────────────────────────────────────────────────────────────┘
```
