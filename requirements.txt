# StudyPal 依赖包列表
# 格式：包名==版本号  # 是否必需

# ==================== 必需包 ====================
requests==2.31.0  # 必需 - 用于调用 AI API
flask==3.0.0  # 必需 - Web 版本需要
flask-cors==4.0.0  # 必需 - CORS 跨域支持
python-dotenv==1.0.0  # 必需 - 环境变量配置

# ==================== 可选包 ====================
# PyQt5==5.15.9  # 可选 - GUI 版本需要
# pygame==2.5.2  # 可选 - 声音播放需要
