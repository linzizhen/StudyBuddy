# StudyPal 依赖包列表
# 格式：包名==版本号  # 是否必需

# ==================== 必需包 ====================
requests==2.31.0  # 必需 - 用于调用 AI API
flask==3.0.0  # 必需 - Web 版本需要

# ==================== 可选包 ====================
# python-dotenv==1.0.0  # 可选 - 用于管理环境变量配置
# 如需要使用 .env 文件存储 API 密钥，请取消注释

# ==================== 未来扩展（暂不需要）====================
# PyQt5==5.15.9  # 可选 - GUI 版本需要
# pygame==2.5.2  # 可选 - 声音播放需要
